# AG-kospi-point 컨텍스트 요약 (CONTEXT_SUMMARY)

## 📌 프로젝트 개요
- **목적**: 2026년 1월 1일부터 현재까지의 대한민국 코스피(KOSPI) 일별 마감가를 수집하여, 텍스트 및 시각화된 HTML 리포트 형식으로 자동 생성하는 프로그램.
- **주요 결과물**:
  - `kospi_closing_prices.txt` (콘솔용 텍스트 문서)
  - `kospi_closing_prices.html` 및 `index.html` (인터랙티브 웹 리포트, GitHub Pages용)

---

## ⚠️ 핵심 요구사항 및 규칙 (사용자 지시사항)
1. **커밋/푸시 정책**: 코딩 작업을 완료한 후 자동으로 테스트하거나 Git 커밋/푸시를 진행하지 않아야 함. **반드시 사용자에게 먼저 확인 및 승인을 받은 후 진행**할 것.
2. **커밋 메시지**: Git 커밋 시 `Co-authored-by` 문구는 절대 사용하지 않을 것.
3. **언어**: 항상 한국어를 사용할 것.

---

## 🚀 주요 기능 및 개발 히스토리

### 1. 데이터 수집 및 계산
- 2025년 마지막 거래일(2025-12-30)의 종가를 가져와, 2026년 첫 거래일의 전일 대비 등락률을 정확하게 계산.
- 상승(빨간색), 하락(파란색), 보합 등락률 및 월별 구분선 제공.

### 2. 이중 데이터 소스 자동 병합 (중요 트러블슈팅)
- **이슈**: `yfinance`나 `FinanceDataReader` 단일 소스 사용 시, 한국 시간 새벽~오전(장 시작 전)에 전일 데이터가 일시적으로 누락되거나 갱신이 지연되는 현상 발생.
- **해결**: `yfinance`(^KS11)와 `FinanceDataReader`(KS11) 두 소스에서 데이터를 동시에 수집하여 **병합(Merge)**. `Close > 0`인 유효한 값만 남겨, 어느 한쪽이 지연되더라도 항상 최신 데이터를 보완 및 유지하도록 구현.

### 3. HTML 리포트 인터랙티브 기능
- **정렬 기능**: `최신순` / `과거순` 데이터를 자바스크립트로 즉각 전환 가능.
- **테마 기능**: CSS 변수를 활용해 `밝게` / `어둡게` 테마 토글 기능 구현.
- **세션 유지**: 사용자가 선택한 테마가 브라우저의 `localStorage`에 저장되어 재방문 시에도 테마 설정 유지 (기본값: 라이트 테마).

### 4. 자동화 및 배포
- **macOS 실행 파일**: Finder에서 더블클릭만으로 가상환경을 활성화하고 데이터를 수집할 수 있는 `run_kospi.command` 제공.
- **GitHub Pages 연동**: 생성된 `index.html`이 GitHub에 푸시되면 즉시 웹에 반영됨 (`https://keynutai.github.io/AG-kospi-point/`).

---

## 📁 주요 파일 구조
- `kospi_fetch.py`: 데이터 수집, 가공 및 파일 생성을 담당하는 메인 Python 스크립트.
- `run_kospi.command`: macOS 환경에서 클릭 한 번으로 실행하기 위한 bash 스크립트.
- `kospi_venv/`: 패키지 의존성이 설치된 가상환경 폴더.
- `README.md`: 프로젝트 소개 및 사용 방법 명시.
